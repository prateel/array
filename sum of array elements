#include <stdio.h>

int main()
{
  int a[5],i , sum;
  for(i=0;i<5;i++);
  {
      scanf("%d",&a[i]);
      sum=sum+a[i];
  }
  for(i=0;i<5;i++){
      printf("%d",a[i]);
  }
  printf("\n%d",sum);
  
  return 0;
}
